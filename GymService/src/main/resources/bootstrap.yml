# application name should be "gym-service" and the application configuration should
# be retrieved from "http://localhost:8888"
spring:
  application:
    name: gym-service
  cloud:
    config:
      failFast: true
      uri: http://configservice:8888